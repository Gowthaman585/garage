# **LEARNED**
### ****getchar() used for reading char one-by-one.****    
###     *if the program reads char from file we must use EOF.*  
###     *if the user give more oversized input overflow occurs.*  
###     *the extra chars are never deleted automatically.*  
###     *they stay in the input buffer.*  
###     *to clear them we need to run a new loop to read remaining char.*  
### ****Pointer****
###     *if we try to insert a direct val to a pointer var.*  
###     *int* \*p *= 2 this make the integer pointer to point the RAM(2).*  
###     *which makes the* \*p *to indicate the mem block of ram whose val is 2.*  
###     *the 2 reprsented in hexadecimal.*  
###     *also the value like 2,6,12 of RAM all point toward deep memory block.*  
###     *which is kernel area,these area for called forbidden area.*  
